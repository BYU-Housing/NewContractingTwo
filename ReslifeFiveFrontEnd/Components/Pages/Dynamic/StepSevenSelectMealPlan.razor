@page "/stepSevenSelectMealPlan"
@inject NavigationManager NavigationManager
@inject IGenService GenService

<h3>Step Seven: Select Meal Plan</h3>

<div class="button-container">
    @for (int i = 1; i <= 10; i++)
    {
        <button class="btn btn-primary m-2" @onclick="() => OnButtonClicked(i)">
            @i
        </button>
    }
</div>

@code {
    private void OnButtonClicked(int buttonNumber)
    {
        Console.WriteLine($"Button {buttonNumber} clicked.");
        // Add any additional logic for handling button clicks here
    }
}
